<h1>Lista de torres</h1>


<ul>
	<% @tower.each do |tower| %>

		<li>
		<p>Nombre de la torre: <%= tower.name %></p>
		<p>Nombre del portero: <%= tower.keeper %></p>
		<%= link_to"editar", edit_tower_path(tower)%> <
		<%= link_to"eliminar", tower_path(tower), method: :delete, data: {confirm: "estas seguro?"} %>
		</li>
	
	<% end %>
</ul>

<%= link_to "Agregar Torre", new_tower_path %>
